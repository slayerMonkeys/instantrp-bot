<nav class="flex items-center justify-between flex-wrap bg-gray-800 p-6">
    <a href="/" class="flex items-center flex-shrink-0 text-white mr-6">
        <img src="<%= bot.user.avatarURL() %>" width="54" height="54" class="fill-current h-10 w-10 mr-2 rounded-full" alt="">
        <span class="font-semibold text-xl tracking-tight"><%= bot.user.username %></span>
    </a>
    <div class="block lg:hidden">
        <button class="flex items-center px-3 py-2 border rounded text-teal-200 border-teal-400 hover:text-white hover:border-white">
            <svg class="fill-current h-3 w-3" viewBox="0 0 20 20" xmlns="http://www.w3.org/2000/svg"><title>Menu</title><path d="M0 3h20v2H0V3zm0 6h20v2H0V9zm0 6h20v2H0v-2z"/></svg>
        </button>
    </div>
    <div class="w-full block flex-grow lg:flex lg:items-center lg:w-auto">
        <div class="text-sm lg:flex-grow">
            <a href="/commands" class="block mt-4 lg:inline-block lg:mt-0 text-white hover:text-gray-400 mr-4">
                Commandes
            </a>
            <% if(user) { %>
                <a href="/home" class="block mt-4 lg:inline-block lg:mt-0 text-white hover:text-gray-400 mr-4">Home</a>
            <% } %>
            <% if(!user) { %>
                <a href="#responsive-header" class="block mt-4 lg:inline-block lg:mt-0 text-white hover:text-gray-400 mr-4">
                    Le serveur RolePlay
                </a>
                <a href="https://discord.gg/tP399Dk" class="block mt-4 lg:inline-block lg:mt-0 text-white hover:text-gray-400">
                    Notre discord
                </a>
            <% } else { %>
                <a href="/patch-note/add" class="block mt-4 lg:inline-block lg:mt-0 text-white hover:text-gray-400">
                    Envoyer un patch-note
                </a>
            <% } %>
        </div>
        <div>
            <% if(user) { %>
                <a href="/logout" class="inline-block text-sm px-4 py-2 leading-none border rounded text-white border-white hover:border-transparent hover:text-gray-800 hover:bg-white mt-4 lg:mt-0">Déconnexion</a>
            <% } else { %>
                <a href="/login" class="inline-block text-sm px-4 py-2 leading-none border rounded text-white border-white hover:border-transparent hover:text-gray-800 hover:bg-white mt-4 lg:mt-0">Connexion Administrateur</a>
            <% } %>
        </div>
    </div>
</nav>
